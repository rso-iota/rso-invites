packages:
    - name: rso-iota-hook
      functions:
        - name: hook-controller
          runtime: python:default
          main: ""
          parameters:
            DISCORD_WEBHOOK_URL: ${DISCORD_WEBHOOK_URL}
